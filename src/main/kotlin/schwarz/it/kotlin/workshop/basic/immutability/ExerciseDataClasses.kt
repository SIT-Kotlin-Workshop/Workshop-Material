package schwarz.it.kotlin.workshop.basic.immutability

// 1. Implement a simple data class "Student" with fields (name as string, age as Int, and grade as string),
//    ensure all properties are read-only (i.e., declared as `val`)

private fun main() {
    // 2. Create an instance of the student class

    // 3. Attempt to modify the student's name

    // 4. Use the .copy() method to create a new instance with a modified grade
}
