package schwarz.it.kotlin.workshop.basic.syntax

// Use `while` that takes an integer `start` and prints a countdown from `start` to 0 using a `while` loop
fun countDown(start: Int) {
}

private fun main() {
    countDown(20)
}
